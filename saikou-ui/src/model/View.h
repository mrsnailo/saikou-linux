#pragma once

/** The app's top-level surfaces, matching the prototype's `NAV` array plus Detail. */
enum class View {
    Home,
    Browse,
    Library,
    Downloads,
    Genres,
    Calendar,
    Settings,
    Details,
};
